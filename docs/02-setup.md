# 2. Setup

## TODO

- Python version and virtualenv instructions
- Required packages (`requirements.txt` or `pyproject.toml`)
- API keys / model provider configuration (`.env.example`)
- Verifying the environment with a smoke test
